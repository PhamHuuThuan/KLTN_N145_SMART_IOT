# MQTT Broker Configuration (Local)
MQTT_HOST=YOUR IP V4 (run docker mqtt)
MQTT_PORT=1883

# Device Configuration
DEVICE_ID=KITCHEN-ESP32-LED1

# Service Configuration
PORT=3000

# Optional: MQTT Authentication (if your broker requires it)
# MQTT_USERNAME=your_username
# MQTT_PASSWORD=your_password

# Note: For local development, use localhost or 127.0.0.1
# Make sure you have a local MQTT broker running (e.g., Mosquitto)
