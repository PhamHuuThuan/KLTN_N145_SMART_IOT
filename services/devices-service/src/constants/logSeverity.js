const LOG_SEVERITY = {
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
  CRITICAL: 'critical'
};

const LOG_SEVERITY_VALUES = Object.values(LOG_SEVERITY);

export { LOG_SEVERITY, LOG_SEVERITY_VALUES };
