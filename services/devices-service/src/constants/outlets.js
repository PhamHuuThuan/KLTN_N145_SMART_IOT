// Centralized outlet identifiers
// Keys are UPPERCASE (for clarity in code), values remain the string ids used in DB and protocol
const OUTLETS = {
  O1: 'o1',
  O2: 'o2',
  O3: 'o3',
  O4: 'o4'
};

const OUTLET_VALUES = Object.values(OUTLETS);

export { OUTLETS, OUTLET_VALUES };
