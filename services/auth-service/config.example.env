# MQTT Broker Configuration (Local)
MQTT_HOST=YOUR IP V4 (run docker mqtt)
MQTT_PORT=1883

# Device Configuration
DEVICE_ID=KITCHEN-ESP32-LED1

# Service Configuration
PORT=3002

# Jwt-secret
JWT_SECRET=your-strong-secret

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/smart_iot

# Email Configuration (SMTP)
# For Gmail, set SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_SECURE=false and use an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=Smart IoT Kitchen <noreply@smartiotkitchen.com>
